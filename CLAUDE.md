# CLAUDE.md

הנחיות עבודה ל-Claude Code בריפו הזה.

## מה זה הפרויקט

סימולציית "פאנל דעות ישראלי" מבוססת LLM: ~220 פרופילים סינתטיים של ישראלים
(מגוונים בגיל, מגזר, נטייה פוליטית ואזור), שנשאלים שאלה דרך Claude API
והתשובות מצטברות לדוח "חכמת ההמונים". פירוט מלא ב-`README.md`.

**אלה לא בני אדם אמיתיים ולא סקר אמיתי.** בכל פלט, סיכום או דוח שמבוסס על
הפאנל יש לציין שזו סימולציה ולא מחקר דעת קהל.

## מבנה

```
panel/generate_profiles.py   # מייצר את מאגר הפרופילים
panel/profiles.json          # 220 הפרופילים (נוצר מראש, בקרת גרסאות)
panel/ask.py                 # שואל שאלה את הפאנל ומדפיס דוח
.claude/agents/israeli-panel.md  # תת-סוכן שמריץ את הפאנל ומסכם
.claude/hooks/session-start.sh   # מתקין תלויות בתחילת סשן (ווב בלבד)
```

## סביבה

- Python 3.11, תלות יחידה: `anthropic>=0.40` (`panel/requirements.txt`).
- בסשן ווב: הוק ה-SessionStart כבר התקין את התלויות - אין צורך להתקין שוב.
- מקומית: `python3 -m pip install -r panel/requirements.txt`.
- נדרש `ANTHROPIC_API_KEY` בסביבה לכל הרצה שפונה ל-API. אם הוא חסר -
  לדווח ולעצור, לא לנסות לעקוף.

## פקודות

```bash
python3 panel/ask.py "השאלה" --limit 25      # בדיקה מהירה וזולה
python3 panel/ask.py "השאלה"                 # הרצה מלאה (~220 קריאות API)
python3 panel/ask.py "השאלה" --output out.json
python3 panel/generate_profiles.py --n 220   # יצירת מאגר מחדש
```

## עלות - חשוב

כל הרצה מלאה = קריאת API לכל פרופיל (~220 קריאות). **תמיד להריץ קודם עם
`--limit 25`** ולוודא שהפלט הגיוני, לפני הרצה מלאה. אל תריץ הרצה מלאה על
דעת עצמך אם לא התבקשת במפורש.

אל תריץ מחדש את `generate_profiles.py` ואל תדרוס את `panel/profiles.json`
בלי בקשה מפורשת - זה קובץ שנמצא בבקרת גרסאות ומשמש בסיס להשוואה בין הרצות.

## בדיקות ולינטר

אין בריפו חבילת בדיקות או לינטר מוגדרים. בדיקת שפיות בסיסית לפני commit:

```bash
python3 -m py_compile panel/ask.py panel/generate_profiles.py
python3 panel/ask.py --help
```

## סגנון

- הקוד, ההערות, ה-README וכל הפלט למשתמש - בעברית.
- מודל ברירת המחדל בסקריפטים: `claude-opus-5` (ניתן לדריסה ב-`ANTHROPIC_MODEL`).
- שגיאות של פרופיל בודד לא מפילות את כל ההרצה - לשמור על הדפוס הזה.
